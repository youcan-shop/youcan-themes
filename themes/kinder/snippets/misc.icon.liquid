{% comment %}
  Renders an icon (HugeIcons)

  Accepts:
  - name: {String} Icon name
  - size: {Number} (optional) Icon size

  Usage:
  {% render 'misc-empty' %}
{% endcomment %}

<i
  class="hgi hgi-stroke hgi-rounded {{ name }}"
  {% if size %}
    style="font-size: {{ size }}px; font-weight: 400;"
  {% endif %}
></i>
