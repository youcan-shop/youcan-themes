{% comment %}
  Renders a color buttons group (Product variant)

  Accepts:
  - options: {Array} variant options
  - name: {String} input name

  Usage:
  {% render 'variant-color', name: 'color', options: [] %}
{% endcomment %}

<yc-variant name="color">
  {% for opt in options %}
    <label for="{{ opt }}" class="variant-item color-item yc-btn icon" style="--variant-color: #669BBC">
      <input type="radio" name="{{ name }}" id="{{ opt }}">
    </label>
  {% endfor %}
</yc-variant>
