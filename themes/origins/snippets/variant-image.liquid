{% comment %}
  Renders a image buttons group (Product variant)

  Accepts:
  - options: {Array} variant options
  - name: {String} input name

  Usage:
  {% render 'variant-image', name: 'image', options: [] %}
{% endcomment %}

<yc-variant name="image">
  {% for opt in options %}
    <label class="variant-item image-item yc-btn icon" for="{{ opt }}">
      <img
        src="{{ "hoodie.webp" | asset_url }}"
        alt="{{ opt }}"
        height="70"
        width="70"
      >
      <input type="radio" name="{{ name }}" id="{{ opt }}">
    </label>
  {% endfor %}
</yc-variant>
