<section class="empty-page">
  {% render 'yc-empty', icon: 'cart', heading: 'Your cart is empty' %}
</section>

{%- schema -%}
{
  "label": "Cart",
  "templates": ["cart"],
  "limit": 1,
  "settings": []
}
{%- endschema -%}
