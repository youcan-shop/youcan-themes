{% comment %}
  Renders an FAQ item (Block)

  Accepts:
    - faq: {Object} Item info

  Usage:
  {% render 'block.faq' %}
{% endcomment %}

<li ui-block="faq" ui-slot="accordion-item" {{ faq.youcan_attributes }}>
  <label ui-slot="accordion-trigger">
    {{ faq.settings.faq_title }}
    <input
      type="checkbox"
      name="faq"
      {% if faq.settings.faq_open_by_default %}
        checked
      {% endif %}
    >
    {% render 'misc.icon', name: 'hgi-arrow-down-01' %}
  </label>
  <div ui-slot="accordion-content" role="region">
    <article>{{ faq.settings.faq_content }}</article>
  </div>
</li>
