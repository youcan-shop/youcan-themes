{% comment %}
  Render a product media (images)

  Usage:
  {% render 'misc.product-media' %}
{% endcomment %}

<ui-carousel ui-product="media" ui-box="media" data-layout="carousel">
  {% if product.images.size > 1 %}
    <div class="markers">
      {% for marker in product.images %}
        <button
          class="marker"
          ui-carousel="marker"
          {% if forloop.first %}
            aria-current="true"
          {% endif %}
          data-image-ratio="{{ settings.product_image_ratio }}"
        >
          <img
            src="{{ marker }}"
            alt="{{ product.name }}"
            height="100%"
            width="100%"
            loading="eager"
          >
        </button>
      {% endfor %}
    </div>
  {% endif %}
  <div
    class="items"
    ui-carousel="wrapper"
    ui-slot="carousel"
    data-orientation="horizontal"
    data-align="center"
    data-image-ratio="{{ settings.product_image_ratio }}"
  >
    {% if product.images.size %}
      {% for image in product.images %}
        <div class="item" ui-carousel="item" ui-slot="carousel-item">
          <img
            src="{{ image }}"
            alt="{{ product.name }}"
            height="100%"
            width="100%"
            {% if forloop.first %}
              loading="eager"
              fetchpriority="high"
            {% else %}
              loading="lazy"
            {% endif %}
          >
        </div>
      {% endfor %}
    {% else %}
      {% render 'misc.image-fallback', icon_size: 60 %}
    {% endif %}
  </div>
</ui-carousel>
